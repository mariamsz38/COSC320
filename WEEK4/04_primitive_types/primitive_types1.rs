// Booleans (`bool`)

fn main() {
    let is_morning = true;
    let is_evening = !is_morning; // Define `is_evening` as the negation of `is_morning`

    if is_morning {
        println!("Good morning!");
    }

    if is_evening {
        println!("Good evening!");
    }
}
